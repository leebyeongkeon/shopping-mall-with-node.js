<!DOCTYPE html>
 <!--  Content (배너이미지) 부분 -->
  <div> <img src="/images/admin_banner2.png" class="img-fluid" alt="배너이미지" width="100%"> </div>

  <!-- 컨텐츠(예, 쇼핑몰일경우 판매제품리스트)헤더부분 -->
   <div class="container-fluid" style="border: solid 20px white; border-radius: 1px">
     <div class="panel panel-default">
   	  <div class="panel-heading">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item">주문 관리</a></li>
            <li class="breadcrumb-item">주문 조회</a></li>
            <!--<li class="breadcrumb-item active" aria-current="page">주문등록관리</li>-->
          </ol>
        </nav>
   	  </div>

      <hr>
      <div class="container">
        <a class="btn btn-default" href="/adminod/form">신규 주문 등록</a>
        <a class="btn btn-default" href="#">주문 정보 수정</a>
        <a class="btn btn-default" href="#">주문 정보 삭제</a>
          <table class="table table-striped">
            <thead>
              <tr>
                <th> 주문번호 </th>
                <th> 송장번호 </th>
                <th> 주문상품 </th>
                <th> 배송업체 </th>
                <th> 주문 날짜 </th>
                <th> 배송예정일 </th>
                <th> 배송상태 </th>
                <th> 내역삭제 </th>
              </tr>
            </thead>
            <tbody>
              <% for(var i = (page * page_num) - page_num; i < (page * page_num); i++) {
                   if(i > length){
                       i++;
                   }else{
                   %>              <tr>
                <td> <%= orderdata[i].orderNum %> </td>
                <td> <a class="btn btn-default" href="/adminod/detail?orderNum=<%= orderdata[i].orderNum %>"> <%= orderdata[i].deliveryNum %> </td>
                <td> <a class="btn btn-default" href="/adminod/detail?orderNum=<%= orderdata[i].orderNum %>"> <%= orderdata[i].orderProductName %> </td>
                <td> <a class="btn btn-default" href="/adminod/detail?orderNum=<%= orderdata[i].orderNum %>"> <%= orderdata[i].deliveryCompany %> </td>
                <td> <a class="btn btn-default" href="/adminod/detail?orderNum=<%= orderdata[i].orderNum %>"> <%= orderdata[i].orderDate %> </td>
                <td> <a class="btn btn-default" href="/adminod/detail?orderNum=<%= orderdata[i].orderNum %>"> <%= orderdata[i].deliveryDate %> </td>
                <td> <a class="btn btn-default" href="/adminod/detail?orderNum=<%= orderdata[i].orderNum %>"> <%= orderArray[i]%></a> </td>
                <td> <a class="btn btn-default" href="/adminod/delete?orderNum=<%= orderdata[i].orderNum %>"> 삭제 </td>
              </tr>
              <% }} %>
            </table>
          </div>

  <p>
    <nav aria-label="...">
    <ul class="pagination justify-content-center">
      <li class="page-item">
        <a class="page-link" href="/adminod/list/<%= previous %>" tabindex="-1" >이전</a>
      </li>
      <% for(var j = 0; j < orderdata.length / page_num; j++){  %>
        <li class="page-item"><a class="page-link" href="/adminod/list/<%= j + 1 %>"><%= j + 1 %></a></li>
      <% } %>
      <li class="page-item">
        <a class="page-link" href="/adminod/list/<%= next %>">다음</a>
      </li>
    </ul>
    </nav>
