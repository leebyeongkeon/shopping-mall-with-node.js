<!DOCTYPE html>

      <hr>
      <div class="container">
        <div class="event_title" style="margin-bottom: 20px;"><h1>이벤트 <span style="font-size:20px; color: #333;">다양한 혜택 얻어가세요!</span></h1></div>

        <div class="row align-items-start">
          <% for(var i =0 ; i< event.length;i++){ %>
          <div class=col-4>
          <div class="card border-muted mb-3" style="max-width: 18rem; max-height: 26rem">
            <div class="card-body text-primary">
              <a href="/event/detail?id=<%=event[i].eventNum%>">
                <img class="img-fluid card-img-top" src=/images/event/<%= event[i].eventImg %>>
                <strong><p style="text-align:center;"><%= event[i].eTitle %></p></strong>
              </a>
                <p style="text-align:center;"><%= event[i].startDATE %>~<%= event[i].endDATE %></p>
            </div>
          </div>
          </div>
          <% } %>
        </div>
      <hr>
      </div>
